spring:
  application:
    name: security-service
  cloud:
    config:
      uri: http://localhost:9000/configService
      fail-fast: true
    discovery:
      enabled:  false
eureka:
  client:
    enabled:  false 
    registerWithEureka: false
    fetchRegistry: false
      
server:
  port: 9090

